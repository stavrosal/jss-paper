/* We want to model a system of food Stores. Each store has a some products (Product) of different categories. Specifically there are 3 categories: -vegetables -fruits -dairy We want a function that returns the total amount of items in store (int) and function that prints all items' names (void). Assume there is a getName() getter in Product class. Also assume that each object (Vegetable, Fruit, Dairy) extends the abstract Product class. */ class Store{ 	List<Vegetable> vegetables; 	List<Fruit> fruits; 	List<Dairy> dairy; 	 	public void addVegetable(Vegetable item){ 		this.vegetables.add(item); 	} 	 	public void addFruit(Fruit item){ 		this.fruits.add(item); 	} 	 	public void addDairy(Dairy item){ 		this.dairy.add(item); 	} 	 	public int getTotalItems(){ 		int total = vegetables.size() + fruits.size() + dairy.size(); 		 		return total; 	} 	 	 	public void printItemNames(){ 		// Supposing that all Product classes have a getName() method 		for (Vegetable v : vegetables){ 			System.out.println(v.getName()); 		} 		 		for (Fruit f : fruits){ 			System.out.println(f.getName()); 		} 		 		for (Dairy d : dairy){ 			System.out.println(d.getName()); 		} 	} } Describe the bad smells you detected
Το ChatGPT είπε:
Here are the code smells present in the given Store class, along with brief explanations:
________________________________________
✅ 1. Code Duplication
You have three separate lists (vegetables, fruits, dairy) and three separate addX() methods.
 You also duplicate loops in printItemNames().
This violates DRY (Don’t Repeat Yourself).
________________________________________
✅ 2. Violation of the Open–Closed Principle (OCP)
If a new product category is introduced (e.g., Meat, Beverage), the following must be changed:
●	add a new list

●	add a new add-method

●	update getTotalItems()

●	update printItemNames()

The class is not closed for modification. This is a major design smell.
________________________________________
✅ 3. Parallel Inheritance Hierarchies
The existence of three separate lists based on the subclass type implies the Store's structure depends on the inheritance structure of Products.
 Every time Product subclasses change, Store must also change.
________________________________________
✅ 4. Primitive Obsession / Type-Specific Collections
Using:
List<Vegetable>
List<Fruit>
List<Dairy>

instead of:
List<Product>

forces the Store to know too much about specific product types — a violation of encapsulation.
________________________________________
✅ 5. Poor Cohesion
The Store class handles too many distinct lists rather than a unified concept of inventory.
 A Store should store Products, not separate category-specific collections.
________________________________________
✅ 6. Hard-coded Logic / Lack of Polymorphism
The methods manually iterate over each specific type:
for (Vegetable v : vegetables) ...
for (Fruit f : fruits) ...
for (Dairy d : dairy) ...

This defeats the purpose of having all products extend an abstract class Product.
 You are not taking advantage of polymorphism at all.
________________________________________
✅ 7. Missing Initialization of Lists
The lists are declared but never initialized:
List<Vegetable> vegetables;

This will throw NullPointerException when calling .add().
________________________________________
Summary of Code Smells
Smell	Description
Duplication	Repeated code for each product type
OCP violation	Must modify class whenever a new product type appears
Parallel inheritance structure	Store depends on Product subclasses
Primitive obsession	Multiple collections instead of one polymorphic collection
Low cohesion	Store handles too many separate structures
No polymorphism	Ignores Product abstraction
Uninitialized collections	Causes runtime errors
________________________________________
If you'd like, I can also provide a refactored version using polymorphism (e.g., a single List<Product>).
Είπες:
Describe the metrics that are affected
Το ChatGPT είπε:
Below are the software quality metrics that are negatively affected by the smells in the given Store class. Each metric corresponds to known object-oriented quality measures (Chidamber & Kemerer, Martin, etc.)
________________________________________
✅ 1. WMC — Weighted Methods per Class (Complexity)
Because Store has separate logic for each product category, the number of methods increases unnecessarily (addVegetable, addFruit, addDairy).
 Each new product type would add even more methods.
→ WMC increases, meaning the class becomes more complex and harder to maintain.
________________________________________
✅ 2. CBO — Coupling Between Objects
Store directly references three specific subclasses:
●	Vegetable

●	Fruit

●	Dairy

If more categories are added, coupling increases even further.
→ CBO is high, indicating the class is tightly coupled to concrete implementations instead of abstractions.
________________________________________
✅ 3. LCOM — Lack of Cohesion of Methods
A cohesive class has methods that operate on the same data.
 Here, methods operate on different lists:
●	printItemNames() touches all lists

●	addVegetable() touches only vegetables

●	addFruit() touches only fruits

●	etc.

The fields are not shared uniformly across methods.
→ LCOM increases, meaning low cohesion.
________________________________________
✅ 4. LOC — Lines of Code
The code is longer than necessary because of repetition:
●	three lists

●	three add methods

●	three loops

●	three places to update item count

If categories increase, LOC grows fast.
→ LOC inflated, harming readability and maintainability.
________________________________________
✅ 5. RFC — Response for a Class
RFC measures how many methods could be invoked in response to a message.
 Because Store defines multiple add-, count-, and print methods, RFC is unnecessarily high.
→ RFC increases, indicating higher surface area and greater testing effort.
________________________________________
✅ 6. Maintainability Index (MI)
Because of:
●	duplicated logic

●	strong coupling

●	low cohesion

●	high LOC

…the maintainability index suffers.
→ MI decreases, meaning the class becomes more expensive to maintain over time.
________________________________________
✅ 7. Extensibility / Modifiability Metrics
This is more qualitative but important:
➤ Adding a new category requires modifying multiple parts of the code (lists, addX(), printing, counting).
 This violates the Open–Closed Principle.
→ Modifiability and extensibility metrics decrease.
________________________________________
Summary Table
Metric	Impact	Why
WMC	↑ increases	More methods needed for each product type
CBO	↑ increases	Store depends on multiple concrete subclasses
LCOM	↑ increases	Methods operate on separate independent lists
LOC	↑ increases	Duplication of methods and loops
RFC	↑ increases	More externally accessible methods
MI	↓ decreases	Combined effect of high LOC + low cohesion + high coupling
Extensibility/Modifiability	↓ decreases	Every new category breaks OCP
________________________________________
If you'd like, I can also explain how refactoring to a single List<Product> would improve each metric, or rewrite the class in a clean, OOP-idiomatic form.
Είπες:
Name which design principle or design pattern could be applied to improve code quality.
Το ChatGPT είπε:
Here are the design principles and design patterns that would most effectively improve the quality of the given code:
________________________________________
✅ 1. SOLID Principles
✔ Open–Closed Principle (OCP)
The store should be open for extension but closed for modification.
 Currently, every new product category forces changes in:
●	fields

●	methods

●	counting logic

●	printing logic

→ A unified structure like List<Product> removes the need to modify the Store when new product types appear.
________________________________________
✔ Liskov Substitution Principle (LSP)
All product subclasses (Vegetable, Fruit, Dairy) should be usable through their abstract base class Product.
→ Replacing multiple lists with a single List<Product> takes full advantage of LSP.
________________________________________
✔ Single Responsibility Principle (SRP)
The Store currently:
●	manages multiple category lists

●	implements separate category-specific logic

●	prints names (presentation concern)

→ Using polymorphism and a single collection reduces responsibility bloat.
________________________________________
✔ DRY Principle (Don’t Repeat Yourself)
The duplication of lists, add-methods, and loops violates DRY.
→ One collection and one addProduct(Product p) method solve this.
________________________________________
✅ 2. Design Patterns
✔ Composite Pattern (mild use)
If products had hierarchical relationships (e.g., product bundles), Composite would allow treating groups and individual items uniformly.
Not strictly required here, but relevant if inventory becomes hierarchical.
________________________________________
✔ Strategy Pattern (for printing or filtering behaviors)
If different output formats or reporting behaviors are needed, Strategy allows plugging in different algorithms without modifying the Store.
E.g.:
●	Print names

●	Print categorized

●	Print alphabetically

________________________________________
✔ Iterator Pattern
Using Iterable<Product> or standard Java Iterator simplifies traversal and encapsulates internal storage.
________________________________________
✔ Factory Method / Abstract Factory (optional)
If object creation for different product categories becomes complex or needs to be abstracted.
Not required here but often recommended in large systems with many product types.
________________________________________
✅ 3. Key Object-Oriented Concept to Apply: Polymorphism
Instead of:
List<Vegetable>, List<Fruit>, List<Dairy>

Use:
List<Product> items;

This allows the code to work through the abstraction, not each concrete type.
________________________________________
📌 Most Important Recommendation
The strongest, most directly applicable principle is:
⭐ Open–Closed Principle (OCP)
→ Refactor to a single List<Product> so the Store doesn’t change when new product categories are added.
And the most relevant pattern is:
⭐ Strategy Pattern (if printing or categorizing policies vary)
→ You can plug in behaviors without modifying the Store.
________________________________________
If you want, I can provide a refactored version of the Store class applying these principles.
Είπες:
Write the code after implementing the solution you chose
Το ChatGPT είπε:
Below is a clean, refactored, OCP-friendly, DRY-compliant version of your code.
 I apply SOLID principles, especially OCP, LSP, SRP, and use a single List<Product> plus polymorphism.
________________________________________
✅ Refactored Code (Simple, Best OOP Practice)
// Base abstract class
abstract class Product {
    private String name;
    
    public Product(String name) {
        this.name = name;
    }
    
    public String getName() {
        return name;
    }
}

// Subclasses
class Vegetable extends Product {
    public Vegetable(String name) {
        super(name);
    }
}

class Fruit extends Product {
    public Fruit(String name) {
        super(name);
    }
}

class Dairy extends Product {
    public Dairy(String name) {
        super(name);
    }
}


// --- Refactored Store ---
class Store {

    // One polymorphic collection (improves OCP, DRY, LSP)
    private List<Product> items;

    public Store() {
        this.items = new ArrayList<>();
    }

    // Single method for all product types
    public void addProduct(Product p) {
        items.add(p);
    }

    // Total count of items
    public int getTotalItems() {
        return items.size();
    }

    // Print all product names polymorphically
    public void printItemNames() {
        for (Product p : items) {
            System.out.println(p.getName());
        }
    }
}

________________________________________
✅ What This Fixes
✔ Removes duplication (DRY)
Only one list, one add method, one print loop.
✔ Applies Open–Closed Principle (OCP)
Add a new product category like:
class Meat extends Product { ... }

→ No changes inside Store.
✔ Uses Liskov Substitution Principle (LSP)
All product types treated as Product.
✔ Improves maintainability & reduces coupling
Store no longer depends on concrete categories.
________________________________________
If you want, I can also show:
🔹 a version using Strategy Pattern for different printing behaviors
 🔹 a version separating printing into its own class (SRP)
 🔹 how to filter by category without breaking OCP
Just tell me!


